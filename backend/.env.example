# ============================================
# FORTUNABET - Variables de Entorno
# ============================================
# Copia este archivo como .env y completa los valores

# --- Base de Datos ---
DATABASE_URL=mongodb+srv://usuario:password@cluster.mongodb.net/?retryWrites=true&w=majority

# --- Servidor ---
PORT=3001
NODE_ENV=development

# --- JWT ---
# Genera una clave secreta segura (mínimo 32 caracteres)
# Puedes usar: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=tu_clave_secreta_muy_segura_aqui

# --- Frontend URL (para CORS y enlaces en emails) ---
FRONTEND_URL=https://tu-dominio.netlify.app

# --- SendGrid (Email) ---
SENDGRID_API_KEY=SG.xxxxxxxxxxxxxxxxxxxxxx
VERIFIED_SENDER_EMAIL=noreply@tudominio.com

# --- Twilio (SMS) ---
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=tu_auth_token_twilio
TWILIO_PHONE_NUMBER=+1234567890

# --- API de Deportes ---
ODDS_API_KEY=tu_api_key_de_the_odds_api

# ============================================
# NOTAS DE SEGURIDAD:
# ============================================
# 1. NUNCA subas el archivo .env a Git
# 2. En producción, usa variables de entorno del servidor (Render, Heroku, etc.)
# 3. Rota las claves periódicamente
# 4. Usa claves diferentes para desarrollo y producción
